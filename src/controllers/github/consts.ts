export const OK_STATUS = [200, 201, 202, 204];

/**
 * Removes all line breaks from text
 */
export const BREAK_REGEX = /(\r\n|\n|\r)/gm;

/**
 * Extracts datetime from text
 */
export const DATE_REGEX = /(\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2})/gm;
